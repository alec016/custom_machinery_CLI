import { ComponentMode } from 'types'

export interface Config {
  default?: ComponentMode // x
  right?: ComponentMode // x
  left?: ComponentMode // x
  top?: ComponentMode // x
  down?: ComponentMode // x
  front?: ComponentMode // x
  back?: ComponentMode // x
  input?: boolean // x
  output?: boolean // x
  enabled?: boolean // x
}
